#!/usr/bin/env python

import os, random, re
print "".join(filter(lambda x: re.match(r'[a-zA-Z0-9-_.]', x), map(lambda x:
    random.choice([0, 1]) and x.upper() or x.lower(),
    os.urandom(256).encode('uu'))))[7:-3]

